/** Estrutura padrão de metadados. */
export interface DefaultMetadata {
	/** Url personalizada. */
	urlId: string;
	/** Timestamp de criação. */
	createdAt: number;
	/** Timestamp de atualização. */
	updatedAt: number;
}
